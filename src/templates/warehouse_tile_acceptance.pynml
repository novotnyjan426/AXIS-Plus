<!--! cargo_amount_accept callback removed.
     Acceptance is now controlled entirely by stop_accept_cargo (per-cargo, no 16-bit packing limit).
     Industries with >4 unique accepted cargos across economies work correctly. -->
